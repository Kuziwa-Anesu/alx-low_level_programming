/*A function to print lower-case alphabet*/
#include "main.h"
/**
 * print_alphabet - prints lower-case alphabet
 *
 */
void print_alphabet(void)
{
	for (i = 97, i <= 122, i++)
		_putchar(i);
	_putchar(10);
}
